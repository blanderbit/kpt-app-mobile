import React, { useState } from 'react';
import { CheckEmailScreenNavigationProp, CheckEmailScreenRouteProp } from "@app/navigation/AppNavigator";

export default function Redirect({ navigation, route }: { navigation: CheckEmailScreenNavigationProp; route: CheckEmailScreenRouteProp }) {
    return (
        <>
            test
        </>
    );
}


